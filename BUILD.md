# 构建指南

## 前提条件

- 安装 Node.js（建议使用最新的 LTS 版本）和 pnpm，使用 `pnpm install` 安装项目依赖。
- 确保你已经安装了 uv 工具，并使用 `uv sync` 同步项目依赖。

## 构建网站

网站构建相关的命令均在 `package.json` 的 `scripts` 中定义。以下是一些常用的构建命令：

| 命令          | 描述                                 |
| ------------- | ------------------------------------ |
| `pnpm dev`    | 启动开发服务器，进行本地开发和调试。 |
| `pnpm build`  | 构建生产版本，生成静态文件。         |
| `pnpm format` | 格式化代码，确保代码风格一致。       |
| `pnpm lint`   | 运行 linter 检查代码质量。           |

## 添加博客发布者

请你将个人相关信息填写到 [`author.ts`](author.ts) 中，并在 `/public/authors` 目录中放置头像图片。

## 常见问题

### 为什么添加发布者后看不到？

你需要重启服务器，或手动将 `author.ts` 加入到 watch 列表中。
